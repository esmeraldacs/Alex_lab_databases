---
title: "Untitled"
output: html_document
date: "2024-11-21"
---

```{r}
library(tidyverse)
```


```{r}
pmodern0 <- read.csv("Pmodern (1).csv")
```


```{r}
pmodern <- pmodern0 |>
  filter(!is.na(ID)) |>
  arrange(ID)
  
#Contat cuantas ID son faltantes
setdiff(seq(1, max(pmodern$ID)), sort(pmodern$ID))
```

Contar cuantas muestras son en total
```{r}
pmodern 

```


buscar por duplicados, al menos se que Cupania esta duplicado
```{r}

```

